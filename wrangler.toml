name = "sctec-static-site"
compatibility_date = "2026-01-11"

[static]
# Deploy repository root as static assets. Adjust to ./dist if you build first.
directory = "./"

# Optional: set account_id in CI as an env var or edit this file before deploy
# account_id = "your-cloudflare-account-id"
